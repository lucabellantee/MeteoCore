/dts-v1/;

/ {
    chosen {
        zephyr,console = &uart0;
        zephyr,shell-uart = &uart0;
    };
};

&uart2 {
    status = "okay";
    current-speed = <115200>;
    tx-pin = <17>;
    rx-pin = <16>;
};